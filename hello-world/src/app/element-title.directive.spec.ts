import { ElementTitleDirective } from './element-title.directive';

describe('ElementTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementTitleDirective();
    expect(directive).toBeTruthy();
  });
});
